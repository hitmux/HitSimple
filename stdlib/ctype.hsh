$ifndef HITSIMPLE_CTYPE_HSH
$define HITSIMPLE_CTYPE_HSH 1

extern is_digit(ch as u8) -> bool
extern is_alpha(ch as u8) -> bool
extern is_alnum(ch as u8) -> bool
extern is_space(ch as u8) -> bool
extern to_upper(ch as u8) -> u8
extern to_lower(ch as u8) -> u8

$endif
